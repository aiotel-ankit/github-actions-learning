/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/github-actions-learning",
    images: {
        unoptimized: true,
      },
};

export default nextConfig;
